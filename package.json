{
  "name": "hive",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm install && cd ../server && npm install",
    "start": "node ./server/dist/server.js"
  },
  "dependencies": {
    "concurrently": "^4.1.2"
  }
}
