{
  "name": "hive",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build && cd ../server && tsc",
    "start": "node ./server/dist/server.js"
  },
  "dependencies": {
    "concurrently": "^4.1.2"
  }
}
